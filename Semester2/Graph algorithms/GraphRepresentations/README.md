The file in.txt contains an undirected graph represented as follows: the first line contains the number of nodes and the following lines the edges of the graph. The program reads the file and represents/stores the graph using the adjacency matrix, the adjacency list and the incidence matrix.